package derealizer.annotation;

import org.junit.jupiter.api.Test;

public class AnnotationTest {

	@Test
	void testSubClass() {
//		SubClass subClass = new SubClass("superField", "subField");
//		byte[] bytes = Derealizer.serialize(subClass, SuperClassEnum.class);
//		SubClass subClass2 = (SubClass) Derealizer.deserialize(bytes, SuperClassEnum.class);
//		assertEquals(subClass.superField(), subClass2.superField());
//		assertEquals(subClass.subField(), subClass2.subField());
	}

}
